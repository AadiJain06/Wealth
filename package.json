{
  "name": "portfolio-analytics",
  "version": "1.0.0",
  "description": "Portfolio Analytics Dashboard - Full Stack Application",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm install --production=false && npm start",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "test": "cd backend && npm test && cd ../frontend && npm test"
  },
  "keywords": [
    "portfolio",
    "analytics",
    "dashboard",
    "react",
    "nodejs",
    "express"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
